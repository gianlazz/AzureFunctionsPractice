beforeAll(async () => {

});
afterAll(async () => {

});

describe("HttpTrigger", () => {
    it("POST", async () => {
        // Arrange

        // Act

        // Assert
    });

    it("GET", async () => {
        // Arrange

        // Act

        // Assert
    });

    it("GET many", async () => {
        // Arrange

        // Act

        // Assert
    });

    it("DELETE", async () => {
        // Arrange

        // Act

        // Assert
    });

    it("PATCH", async () => {
        // Arrange

        // Act

        // Assert
    });
})